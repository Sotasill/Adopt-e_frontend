export const selectRole = (state) => state.registration?.role;
export const selectIsLoading = (state) => state.registration?.isLoading;
export const selectError = (state) => state.registration?.error;
